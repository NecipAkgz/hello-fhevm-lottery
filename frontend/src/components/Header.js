const Header = () => {
  return (
    <header className="panel hero-panel">
      <div className="hero">
        <span className="hero-eyebrow">FHEVM / Private Draw</span>
        <h1 className="hero-title">Confidential Lottery</h1>
        <p className="hero-lede">Encrypted tickets, transparent prize pools, and on-chain fairness in one lean dashboard.</p>
        <div className="hero-divider" />
        <p className="hero-meta">Fully homomorphic encryption keeps entries hidden until a winner is confirmed.</p>
      </div>
    </header>
  );
};

export default Header;
